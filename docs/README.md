# orc-brain docs

The full technical specification lives in **`SPEC.md`** (added in the next
commit). `SPEC.md` is the source of truth for architecture, components, and
behavior — this scaffold intentionally contains only structure and stubs.

Until then, see the root [`README.md`](../README.md) for an overview.
