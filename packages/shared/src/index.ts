/** Shared types and event schemas for orc-brain. See docs/SPEC.md (source of truth). */

/** Semantic version of the shared contract. Bumped when event schemas change. */
export const SHARED_SCHEMA_VERSION = "0.0.0" as const;

// TODO: shared domain types (Run, Task, Worker, ...) and event schemas.
